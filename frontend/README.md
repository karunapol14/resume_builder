# Frontend - Resume Builder

Setup

1. Install dependencies:

```powershell
cd frontend
npm install
```

2. Create `.env` with:

```
VITE_API_BASE=http://localhost:5000
```

3. Run dev server:

```powershell
npm run dev
```
